import distutils.core
import pathlib
import numpy

distutils.core.setup(name = 'scanner_lifespans',
        version = '1.0',
        description = 'scanner lifespan analysis package',
        packages = ['scanner_lifespans'])
